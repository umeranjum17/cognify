{
  "name": "cognify-flutter-backend",
  "version": "1.0.0",
  "description": "Vercel Edge Functions for Cognify Flutter app configuration",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@vercel/edge": "^1.1.1",
    "firebase-admin": "^12.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
