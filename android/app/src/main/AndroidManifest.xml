<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Remove Foreground service permissions; app does not run a user-visible foreground service -->
    <!-- Play Console requires declaration if present, so we remove them to avoid FGS policy prompts -->
    <!-- If background service is enabled later, re-add specific foregroundServiceType and declare in Console -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Network Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Billing permission for Google Play purchases (per RevenueCat docs) -->
    <uses-permission android:name="com.android.vending.BILLING" />
    
    <!-- Background Processing - Optional, gracefully handled -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    
    <!-- Advertising ID permission required for Firebase Analytics and Google Sign-In -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <!-- Notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Queries for url_launcher to work on Android 11+ -->
    <queries>
        <!-- For opening web URLs -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <!-- For opening custom schemes -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="*" />
        </intent>
        <!-- Visibility of Play Store app/package for billing flows on Android 11+ -->
        <package android:name="com.android.vending" />
    </queries>

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:screenOrientation="portrait"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Enable Flutter deep linking -->
            <meta-data
                android:name="flutter_deeplinking_enabled"
                android:value="true" />
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Intent filter for sharing text/URLs from other apps -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- Intent filter for sharing multiple items -->
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- Intent filter for viewing URLs -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
            </intent-filter>

            <!-- HTTPS App Link for OAuth callback (use stable prod domain) -->
            <!-- TODO: Replace 'cognify.app' with your production domain if different -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="cognify.app"
                    android:pathPrefix="/openrouter/callback" />
            </intent-filter>

            <!-- Custom scheme deep link fallback used by the callback HTML -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="cognify"
                    android:host="oauth"
                    android:pathPrefix="/callback" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Advertising ID configuration for Firebase Analytics -->
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="true" />
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />
        
        <!-- Explicitly declare no foreground services are used -->
        <meta-data
            android:name="com.google.android.gms.ads.DELAY_APP_MEASUREMENT_INIT"
            android:value="true" />
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="true" />
        
        <!-- Foreground Service Declaration - explicitly state none are used -->
        <meta-data
            android:name="android.app.uses_foreground_service"
            android:value="false" />
        
        <!-- Remove flutter_background_service declarations to avoid FGS requirements -->
        <receiver
            android:name="id.flutter.flutter_background_service.WatchdogReceiver"
            tools:node="remove" />
        <receiver
            android:name="id.flutter.flutter_background_service.BootReceiver"
            tools:node="remove" />
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            tools:node="remove" />
    </application>
</manifest>
