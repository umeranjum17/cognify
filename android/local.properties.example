# Flutter SDK path (automatically set by Flutter)
flutter.sdk=/path/to/flutter

# Android SDK path (automatically set by Android Studio)
sdk.dir=/path/to/android/sdk

# Release signing configuration
# Copy this file to local.properties and fill in your actual values
# DO NOT commit local.properties to version control

# Keystore file path (absolute or relative to android/app directory)
storeFile=/Users/umerfaroq/cognify-release-key.jks

# Keystore password
storePassword=YOUR_KEYSTORE_PASSWORD

# Key alias
keyAlias=YOUR_KEY_ALIAS

# Key password
keyPassword=YOUR_KEY_PASSWORD

# Example:
 mdfind "kMDItemFSName == '.jks'" mdfind "kMDItemFSName == '.keystore'"# storeFile=/Users/umerfaroq/cognify-release-key.jks
# storePassword=mySecurePassword123
# keyAlias=cognify-key
# keyPassword=mySecurePassword123
